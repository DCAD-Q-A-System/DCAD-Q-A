export enum USER_TYPE {
  ADMIN = "ADMIN",
  PANELLIST = "PANELLIST",
  STUDENT = "STUDENT",
  GUEST = "GUEST",
}

export enum CATEGORIES {
  POPULAR = "POPULAR",
  RECENT = "RECENT",
  OLDER = "OLDER",
}

export enum VARIANT {
  SUCCESS = "success",
  DANGER = "danger"
}